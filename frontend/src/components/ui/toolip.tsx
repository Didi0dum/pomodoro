import React from "react";

export const TooltipProvider: React.FC<{ children?: React.ReactNode }> = ({ children }) => {
  // Minimal stub so imports resolve. Replace with Radix / real provider later.
  return <>{children}</>;
};